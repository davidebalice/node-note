<nav class="navbar navbar-expand-sm navbar-light bg-success mb-5 w_100">
  <a class="navbar-brand" href="/">My Note<img  src="img/logo1.png"  class="responsive" alt="">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="{{base_url}}">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{base_url}}/info">Info</a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
{{#if user}}
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" 
          id="navbarDropdownMenuLink">Note</a>
          <div class="dropdown-menu">
            <a href="{{base_url}}/notes" class="dropdown-item">Lista Note</a>
            <a href="{{base_url}}/add/note" class="dropdown-item">Nuova nota</a>
          </div>
        </li>
            <li class="nav-item">
            <a href="{{base_url}}/logout" class="nav-link">Logout</a>
          </li>
        {{else}}
       <li class="nav-item">
            <a href="{{base_url}}/login" class="nav-link">Login</a>
          </li>
          <li class="nav-item">
            <a href="{{base_url}}/register" class="nav-link">Registrazione</a>
          </li>
          {{/if}}
      </ul>
    </div>
  </div>
</nav>